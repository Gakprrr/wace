upstream nextjs_upstream {
  server next-app:3000;
  keepalive 64;
}

server {
  listen 80;
  server_name wace.store www.wace.store;
  # En prod avec SSL, décommentez la ligne suivante :
  # return 301 https://$server_name$request_uri;

  # Mode HTTP seul pour dev
  location / {
    proxy_pass         http://nextjs_upstream;
    proxy_http_version 1.1;
    proxy_set_header   Upgrade $http_upgrade;
    proxy_set_header   Connection 'upgrade';
    proxy_set_header   Host $host;
    proxy_set_header   X-Real-IP $remote_addr;
    proxy_cache_bypass $http_upgrade;
  }
}

# Configuration SSL (commentée pour pouvoir lancer sans certificats localement)
# server {
#   listen 443 ssl http2;
#   server_name wace.store www.wace.store;
#
#   ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#   ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#   ssl_protocols       TLSv1.2 TLSv1.3;
#   ssl_ciphers         HIGH:!aNULL:!MD5;
#
#   # Security headers (OWASP)
#   add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#   add_header X-Content-Type-Options nosniff always;
#   add_header X-Frame-Options DENY always;
#   add_header X-XSS-Protection "1; mode=block" always;
#   add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#   add_header Content-Security-Policy "default-src 'self'; img-src 'self' res.cloudinary.com; ..." always;
#
#   # Gzip
#   gzip on;
#   gzip_types text/html application/javascript application/json text/css;
#
#   location / {
#     proxy_pass         http://nextjs_upstream;
#     proxy_http_version 1.1;
#     proxy_set_header   Upgrade $http_upgrade;
#     proxy_set_header   Connection 'upgrade';
#     proxy_set_header   Host $host;
#     proxy_set_header   X-Real-IP $remote_addr;
#     proxy_cache_bypass $http_upgrade;
#   }
#
#   # Cache static assets
#   location /_next/static/ {
#     proxy_pass http://nextjs_upstream;
#     add_header Cache-Control "public, max-age=31536000, immutable";
#   }
# }
